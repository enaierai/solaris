# Sunucunun Rewrite (Yeniden Yazma) motorunu aç
RewriteEngine On
# Projenin ana klasörünü belirt (Bu, XAMPP gibi alt klasörlerde çalışırken hayat kurtarır)
RewriteBase /solaris/

# Eğer istenen şey, var olan bir dosya DEĞİLSE...
RewriteCond %{REQUEST_FILENAME} !-f
# Ve eğer istenen şey, var olan bir klasör DEĞİLSE...
RewriteCond %{REQUEST_FILENAME} !-d

# ...o zaman gelen tüm istekleri, URL'yi bir parametre olarak ekleyerek index.php'ye yönlendir.
RewriteRule ^(.*)$ index.php?url=$1 [L,QSA]